---
description: Spawn verify-app agent. Builds, launches, screenshots, kills.
argument-hint: "[all-variants]"
---

Spawn the `verify-app` subagent. If `$ARGUMENTS` contains `all-variants`, instruct it to drive the full windowStyle × backgroundStyle × tone matrix. Otherwise verify current preferences only.

Relay the agent's report verbatim.
